(
  (
    (edi <- 3)
    (edi *= 2)
    (edi += 1)
    (ebx <- :one)
    (call ebx)
    (eax <- (print edi))
  )
  (:two
    (ebx <- edi)
    (esi <- esi)
    (eax <- 7)
    (eax <- 5)
    (eax <- (print eax))
    (edi <- ebx)
    (esi <- esi)
    (return)
  )
  (:one
    (edi <- edi)
    (esi <- esi)
    (eax <- 3)
    (ebx <- 1)
    (ebx <- eax)
    (eax <- ebx)
    (eax <- (print eax))
    (call :two)
    (edi <- edi)
    (esi <- esi)
    (return)
  )
)