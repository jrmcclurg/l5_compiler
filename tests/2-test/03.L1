(
  (
    (edi <- 4)
    (eax <- 9)
    (esi <- eax)
    (esp -= edi)
    ((mem ebp -4) <- esi)
    (eax <- (print esi))
    (call :one)
    (esi <- (mem ebp -4))
    (eax <- esi)
    (esp += edi)
    (eax <- (print eax))
  )
  (:two
    (esp -= 4)
    (ebx <- edi)
    ((mem ebp -4) <- esi)
    (esp -= 4)
    ((mem ebp -4) <- esi)
    (esi <- 5)
    (eax <- (print esi))
    (esi <- (mem ebp -4))
    (esp += 4)
    (edi <- ebx)
    (esi <- (mem ebp -4))
    (return)
  )
  (:one
    (esp -= 4)
    (edi <- edi)
    ((mem ebp -4) <- esi)
    (esp -= 4)
    ((mem ebp -4) <- esi)
    (esi <- 3)
    (eax <- (print esi))
    (call :two)
    (esi <- (mem ebp -4))
    (esp += 4)
    (edi <- edi)
    (esi <- (mem ebp -4))
    (return)
  )
)