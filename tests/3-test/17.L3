;; EECS 322 Compiler Construction
;; Test-3 Assignment (Test Cases for L3)
;; 5-3-12

;; Simple L3 program which does some comparisons
(
  ;; this is the program entry point
  (let ([testa (< 13 15)])
  (let ([temp (print testa)])
  (let ([testa (< 15 15)])
  (let ([temp (print testa)])
  (let ([testa (< 17 15)])
  (let ([temp (print testa)])
  (let ([testb (<= 23 15)])
  (let ([temp (print testb)])
  (let ([testc (= 15 15)])
  (let ([temp (print testc)])
  (let ([testd (= 19 15)])
  (let ([temp (print testd)]) 0 ))))))))))))
)
