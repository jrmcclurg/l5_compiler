;; EECS 322 Compiler Construction
;; Test-3 Assignment (Test Cases for L3)
;; 5-3-12

;; Simple L3 program which demonstrates interesting 
;; array behavior
(
  ;; this is the program entry point
  (let ([arr (new-array 6 0)])
  (let ([temp (print arr)])
  (let ([arr (new-array 1 0)])
  (print arr) )))
)
